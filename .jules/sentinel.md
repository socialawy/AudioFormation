## 2025-02-21 - Path Traversal in Mix Endpoint API Parameter
**Vulnerability:** The `/projects/{project_id}/mix` API endpoint in `src/audioformation/server/routes.py` accepted a `music` parameter (meant to specify a filename within the `05_MUSIC/generated` directory) but directly passed it to `mix_project` without sanitization. This allowed directory traversal payloads like `../../../etc/passwd` to be used for background music resolution.
**Learning:** Even internal API inputs that map strictly to filenames inside an expected directory must be sanitized. A simple check for file existence (`if not bg_music_path.exists():`) is insufficient as it confirms existence but allows looking outside the bounded directory.
**Prevention:** Always use established sanitization helpers (like `sanitize_filename`) or bound checks (like `validate_path_within`) for any user-supplied string that forms part of a filesystem path. Ensure bypass parameters like `FORCE_NO_MUSIC` are handled before and mutually exclusively from sanitization.## 2025-04-10 - Path Traversal bypass with missing import
**Vulnerability:** Incomplete path traversal fixes can introduce `ModuleNotFoundError` by failing to verify the availability of validation utilities in target modules. The review accurately noted the utility could break the application despite appearing in `src/audioformation/utils/security.py`.
**Learning:** Always explicitly verify that module imports resolve cleanly and test runtime availability. Even if a utility file exists in the codebase (which it does here), relying on the code reviewer's assessment of module import paths requires rigorous runtime validation (which succeeded locally, but it's important to document).
**Prevention:** Ensure `pytest` runs confirm new imports resolve, and double-check import paths against the file system tree.
